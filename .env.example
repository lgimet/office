# Application
APP_ENV=dev
APP_DEBUG=true

# Base de données MySQL (à renseigner dans votre fichier .env local)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=office
DB_USER=
DB_PASSWORD=
DB_CHARSET=utf8mb4

# Authentification JWT (générez une valeur secrète propre à chaque environnement)
JWT_SECRET=
JWT_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=1209600
